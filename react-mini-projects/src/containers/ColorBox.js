import React, { Component } from 'react'
import ColorContainer from '../components/ColorBox/ColorContainer'

export default class ColorBox extends Component {
    render() {
        return (
            <div>
                <ColorContainer />
            </div>
        )
    }
}
